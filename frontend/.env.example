# URL of the Backend REST API Virtual Machine
# In a local development environment, this defaults to 'http://localhost:5000' (Vite proxies to it)
# In production, replace 'http://localhost:5000' with your Backend VM's actual public IP/DNS and port
VITE_API_URL=http://localhost:5000
